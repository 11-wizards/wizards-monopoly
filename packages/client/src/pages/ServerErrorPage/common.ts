import { defineMessages } from 'react-intl';

export const serverErrorPageText = defineMessages({
  textErrorMessage: {
    id: 'server.error.message',
  },
  buttonText: {
    id: 'server.error.message.button-text',
  },
});
