import { defineMessages } from 'react-intl';

export const ClientErrorPageText = defineMessages({
  textErrorMessage: {
    id: 'client.error.message',
  },
  buttonText: {
    id: 'client.error.message.button-text',
  },
});
