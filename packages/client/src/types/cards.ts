export type CardsData = {
  imgSrc: string;
  price?: number;
  priceView: string;
  title: string;
};

export type CardData = {
  img: CanvasImageSource | null;
  priceView: string;
  title: string;
};
