pre-commit:
  parallel: true
  commands:
    frontend-linter:
      root: "packages/client/"
      glob: '*.{ts,tsx}'
      run: yarn lint {staged_files}
    backend-linter:
        root: "packages/server/"
        glob: '*.{ts,tsx}'
        run: yarn lint {staged_files}
